<nav class="header">
  <a href="/" class="home-link">drhayes.io</a>
  <ul class="menu open-face">
    {% for item in menu %}
      <li>
        <a href="{{ item.url }}">{{ item.name }}</a>
      </li>
    {% endfor %}
  </ul>
  <div class="hamburger">
    <details>
      <summary>{% include "hamburger.svg" %}</summary>
      <ul>
        {% for item in menu %}
        <li>
          <a href="{{ item.url }}">{{ item.name }}</a>
        </li>
        {% endfor %}
      </ul>
    </details>
  </div>
</nav>
