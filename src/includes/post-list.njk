<ol class="list-style-type:none h-feed" style="padding-inline-start:0;">
  {%- for post in postList | sort -%}
  <li class="two-line h-entry">
    <div>
      {%- if post.data.updated | isValidIsoDate %}
      <time class="date dt-updated">{{ post.data.updated | dateFormat }}</time>
      {%- elif post.data.date | isValidIsoDate %}
      <time class="date dt-published">{{ post.data.date | dateFormat }}</time>
      {% endif -%}
    </div>
    <div>
      <a href="{{ post.url }}" class="u-url p-name">{{ post.data.title }}</a>
      {% if post.data.draft %}
      {% include "draft-badge.njk" %}
      {% endif %}
    </div>
  </li>
  {%- endfor -%}
</ol>

