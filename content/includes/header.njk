<nav class="mb-8 flex md:flex-col">
  <div class="flex-1 md:flex-initial md:w-1/3">
    <a href="/" name="drhayes.io home page">
      {% include "raven.svg" %}
    </a>
  </div>

  <ul class="flex-1 flex flex-col justify-evenly text-center w-full md:flex-row md:mt-6 md:justify-start">
    {% for item in menu %}
    <li class="mr-4">
      <a class="" href="{{ item.url }}">{{ item.name }}</a>
    </li>
    {% else %}
      <li>Nuthin'</li>
    {% endfor %}
  </ul>
</nav>
