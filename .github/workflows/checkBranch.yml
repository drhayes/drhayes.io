name: Does it build?

on:
  push:
    branches-ignore:
      - master

jobs:
  deploy:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master

    - name: Setup Hugo
      uses: peaceiris/actions-hugo@v2.2.2
      with:
        hugo-version: 'latest'
        extended: true

    - name: Build
      run: hugo --gc --minify -v
